# Meme Machine environment variables.
# Copy to .env.local and fill in as features land. Nothing is required for V1.
#
# Supabase — Stage 6 (curation flywheel: swipe votes on generated questions).
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
