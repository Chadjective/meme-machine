# Normalize line endings: store LF in the repo, so CI (Linux) and Windows agree.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.woff binary
*.woff2 binary
*.ico binary
